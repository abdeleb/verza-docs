---
hide_table_of_contents: true
---

import ClientTag from '@site/src/components/tags/ClientTag';
import ServerTag from '@site/src/components/tags/ServerTag';

`Icosahedron` is a primitive object that creates a icosahedron.

### Syntax

```jsx
<Icosahedron />
```

### Parameters

All objects share the same base parameters from [`ComponentObjectProps`](/sdk/types/ComponentObjectProps).

| Name       | Type                                                | Description                    |
| ---------- | --------------------------------------------------- | ------------------------------ |
| `radius`   | `number`                                            | The radius of the icosahedron. |
| `detail`   | `number`                                            | The detail of the icosahedron. |
| `bloom`    | `boolean`                                           | Whether use bloom effect.      |
| `color`    | [`ColorType`](/sdk/types/ColorType)                 | The base color.                |
| `material` | [`ObjectMaterialMix`](/sdk/types/ObjectMaterialMix) | The material.                  |

### Example

```jsx
<Icosahedron radius={1} detail={0} color="red" />
```

**Available on:** <ClientTag /> <ServerTag />
