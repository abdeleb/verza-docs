---
hide_table_of_contents: true
---

import ClientTag from '@site/src/components/tags/ClientTag';

Adds animations to the engine.

:::caution Heads Up!
This hook do not play animations to the player, it only adds it to the engine. To play animations to the player, use [`player.playAnimation`](/sdk/managers/player/playAnimation).
:::

### Syntax

```js
useAddAnimations(url);
```

### Parameters

| Name  | Type     | Description                           |
| ----- | -------- | ------------------------------------- |
| `url` | `string` | The URL of the animation file to add. |

### Returns

- [`AnimationInfo[]`](/sdk/types/AnimationInfo) - The added animations.

### Example

```js
const anims = useAddAnimations('https://example.com/animation.glb');

console.log('Animations added:', anims);
```

**Available on:** <ClientTag />
