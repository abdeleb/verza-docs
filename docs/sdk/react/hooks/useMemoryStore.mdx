---
hide_table_of_contents: true
---

import ClientTag from '@site/src/components/tags/ClientTag';

Gets a memory store manager.

### Syntax

### Parameters

| Name    | Type     | Description                                        |
| ------- | -------- | -------------------------------------------------- |
| `name`  | `string` | The name of the memory store.                      |
| `scope` | `string` | The scope of the memory store. (default: `global`) |

### Returns

- [`MemoryStoreManager`](/sdk/types/MemoryStoreManager) - The memory store manager.

### Example

```js
const store = useMemoryStore('scores');
```

**Available on:** <ClientTag />
