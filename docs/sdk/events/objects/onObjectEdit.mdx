---
hide_table_of_contents: true
---

import ClientTag from '@site/src/components/tags/ClientTag';

This event is triggered when an object is edited.

### Parameters

| Name           | Type                                                      | Description                       |
| -------------- | --------------------------------------------------------- | --------------------------------- |
| `object`       | `ObjectManager`                                           | The object being edited           |
| `type`         | [`ObjectEditActionType`](/sdk/types/ObjectEditActionType) | The type of edit action performed |
| `prevTransform | [`ObjectEditTransform`](/sdk/types/ObjectEditTransform)   | The old transform of the object   |

### Example

```js
engine.events.on('onObjectEdit', (object, type, prevTransform) => {
  console.log('Object edited:', object, 'Edit action:', type, prevTransform);
});
```

**Available on:** <ClientTag />
