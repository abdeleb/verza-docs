---
hide_table_of_contents: true
---

import ClientTag from '@site/src/components/tags/ClientTag';
import ServerTag from '@site/src/components/tags/ServerTag';

This event is triggered when an entity is destroyed.

### Parameters

| Name     | Type                                        | Description                    |
| -------- | ------------------------------------------- | ------------------------------ |
| `entity` | [`EntityManager`](/sdk/types/EntityManager) | The entity that was destroyed. |

### Example

```js
engine.players.events.on('onDestroy', player => {
  console.log('A player has been destroyed with id: ' + player.id);
});
```

**Available on:** <ClientTag /> <ServerTag />
