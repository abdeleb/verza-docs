---
hide_table_of_contents: true
---

import ClientTag from '@site/src/components/tags/ClientTag';

Create a sound attached to a player.

### Syntax

```js
engine.audio.createPlayerSound(player, soundName, options, withId);
```

### Parameters

| Name        | Type                                                    | Description                                             |
| ----------- | ------------------------------------------------------- | ------------------------------------------------------- |
| `player`    | `string` or [`PlayerManager`](/sdk/types/PlayerManager) | The player id or player manager to attach the sound to. |
| `soundName` | `string`                                                | The name of the sound to create.                        |
| `options`   | [`SoundOptions`](/sdk/types/SoundOptions)               | The options to use for the sound. (optional)            |
| `withId`    | `string`                                                | The ID of the sound to create. (optional)               |

### Returns

- [`SoundManager`](/sdk/types/SoundManager) - The sound manager.

### Example

```js
const audio = engine.audio.createPlayerSound(player, 'fire', {
  position: [0, 2, 0],
});

audio.play();
```

**Available on:** <ClientTag />
