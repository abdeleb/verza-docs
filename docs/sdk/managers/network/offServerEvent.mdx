---
hide_table_of_contents: true
---

import ServerTag from '@site/src/components/tags/ServerTag';
import ClientTag from '@site/src/components/tags/ClientTag';

Unregister a listener function previously registered with `onServerEvent`.

### Syntax

```js
engine.network.offServerEvent(event, listener);
```

### Parameters

| Name       | Type                                | Description                                         |
| ---------- | ----------------------------------- | --------------------------------------------------- |
| `event`    | `string`                            | The event name to unsubscribe.                      |
| `listener` | `(data?: NetworkEventData) => void` | The listener to remove from the event subscription. |

### Example

```js
// subscribe
const listener = engine.network.offServerEvent('onDoorOpen', () => {
  // ...
});

// unsubscribe
engine.network.offServerEvent('onDoorOpen', listener);
```

**Available on:** <ClientTag /> <ServerTag />
