```ts
class PlayerVoicechatManager {
  setPlayerToPlayerMode(
    playerId: number | PlayerManager,
    mode: VoicechatModeType | null,
  ): void;

  enableChannel(channel: number): void;

  disableChannel(channel: number): void;

  setMuted(muted: boolean): void;

  setDistance(distance: number | null): Promise<void>;
}
```
