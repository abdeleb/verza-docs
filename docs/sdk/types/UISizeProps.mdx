```ts
type UISizeProps = {
  height: UISizePropValue;
  width: UISizePropValue;
  left?: UISizePropValue;
  top?: UISizePropValue;
  right?: UISizePropValue;
  bottom?: UISizePropValue;
  zIndex?: number;
};
```

```ts
type UISizePropValue =
  | `${number}vh`
  | `${number}vw`
  | `${number}px`
  | `${number}svw`
  | `${number}svh`
  | `${number}lvw`
  | `${number}lvh`
  | `${number}dvw`
  | `${number}dvh`
  | `${number}%`
  | 'auto'
  | 'none';
```
